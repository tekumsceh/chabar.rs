# Copy to .env.deploy (gitignored) and edit.
# Prefer an SSH config Host alias (see scripts/deploy.ps1 header).

DEPLOY_HOST=chabar
DEPLOY_PATH=/var/www/ioorganize

# Only if DEPLOY_HOST is a bare IP/hostname without User in ~/.ssh/config:
# DEPLOY_USER=root
